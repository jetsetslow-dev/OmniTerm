<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <!-- Deterministic SAF endpoint for opt-in physical tests; absent from release builds. -->
        <provider
            android:name="com.jetsetslow.omniterm.SlowTransferContentProvider"
            android:authorities="${applicationId}.slowtransfer"
            android:exported="false" />
    </application>
</manifest>
